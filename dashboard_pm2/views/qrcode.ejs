<%- include('partials/header') %>

<div class="container py-5 d-flex justify-content-center">
  <div class="card shadow w-100" style="max-width: 800px;">
    <div class="card-header bg-primary text-white">
      <h6 class="mb-0 fw-semibold">QR Code Access</h6>
    </div>
    <div class="card-body text-center">
      <p class="fw-semibold mb-2">Whatsapp refreshes QR Codes every 8 secs,<br>so get your phone ready to scan first.</p>
      <p class="text-muted mb-4">Click the button below to access the QR code page:</p>
      <a href="/qr-code" class="btn btn-primary px-4 py-2">Open QR Code Page</a>
    </div>
  </div>
</div>

<%- include('partials/footer') %>